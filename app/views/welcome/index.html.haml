%p= "hello world"

- @user = User.new
= simple_form_for @user, url: user_path(id: 1), method: :get do |f|
  = f.input :email
  = f.input :password
  = f.submit "Log in"

= link_to "Sign Up", new_user_path
